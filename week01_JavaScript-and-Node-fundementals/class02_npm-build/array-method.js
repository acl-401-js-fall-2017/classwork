
let start = 0;
if (initValue === undefined) {
    initValue = array[0];
    start = 1;
}
const accumulator = initValue;

for(let i = start; i < length; i++) {

}

[1, 4, 5].indexOf(4)

.findIndex(person => person._id === match)
